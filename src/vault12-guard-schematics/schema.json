{
  "$schema": "http://json-schema.org/schema",
  "$id": "NgxsMigrationSchematic",
  "title": "NGXS Migration Schema",
  "type": "object",
  "properties": {
    "migrationName": {
      "type": "string",
      "description": "The name of the migration",
      "x-prompt": "What name would you like to give to this migration?"
    }
  },
  "required": ["migrationName"]
}